enum AlgorithmType {
  search,
  sort,
}

enum SearchType {
  linear,
  binary,
}

enum SortType {
  bubble,
  insertion,
  selection,
  quick,
  merge,
  counting,
  radix,
  heap,
}

enum OrderType {
  ascending,
  descending,
}
